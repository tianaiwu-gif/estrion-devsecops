apiVersion: v1
kind: Service
metadata:
  name: {{ include "estrion-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "estrion-app.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
